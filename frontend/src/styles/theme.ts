import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#2f4f7f', // Slate
    },
    secondary: {
      main: '#008000', // Emerald
    },
  },
});

export default theme;